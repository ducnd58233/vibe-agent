# Stack profiles

Pinned **repository-specific** stack notes: frameworks, paths, tooling, and boundaries. Skills under [`skills/`](../skills/ROUTER.md) stay generic; **open profiles from here** when you need concrete stack answers.

## Start here

<references>

1. **[`ROUTER.md`](ROUTER.md)** — authoritative index of all profile files in this folder; compose **multiple** profiles when a task crosses layers (e.g. web + API + infra).
2. **[`TEMPLATE.md`](TEMPLATE.md)** — authoring contract when you add **`*.md`** profiles (required headings, ROUTER sync).

Without a matching profile row yet, inspect manifests (`package.json`, `pyproject.toml`, `go.mod`, …) and package layout (`apps/`, `backend/`, `services/`); propose a new profile from `TEMPLATE.md` when the repo stabilizes enough to document.
</references>

## Product charter

<context>

Assistant stance and domain rules stay in root [`AGENTS.md`](../../AGENTS.md)—not duplicated in profiles unless a profile cites a carve-out.
</context>
